/**
 * This files contains the configuration for the routes.
 */

export default {

    /**
     * The files that contain the routes.
     * These files will be imported and the routes will be registered.
     * The files should export a default function that returns the routes.
     * The files should return a Route or RouteGroup instance.
     */
    files: [
        'src/routes/routes.ts',
    ]
}