{
  "name": "nodevel",
  "version": "1.0.0",
  "description": "A framework for backend web server",
  "main": "index.js",
  "bin": {
    "nodevel": "dist/init.js"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc --project tsconfig.cli.json && echo '#!/usr/bin/env node' | cat - dist/init.js > temp && mv temp dist/init.js && chmod +x dist/init.js",
    "prepublishOnly": "npm run build"
  },
  "author": "Nicolas Oleinizak",
  "license": "ISC",
  "dependencies": {
    "commander": "^14.0.0",
    "fs-extra": "^11.3.0",
    "path": "^0.12.7",
    "url": "^0.11.4"
  },
  "devDependencies": {
    "tsx": "^4.20.3",
    "typescript": "^5.8.3"
  },
  "type": "module"
}
